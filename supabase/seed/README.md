# Supabase Seed Data

Contains sample test data fixtures for local testing and automated E2E validation.
Seed data is executed via `npx supabase db reset` or loaded directly during development testing.
